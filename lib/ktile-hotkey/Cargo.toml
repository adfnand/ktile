[package]
name = "ktile-hotkey"
version = "0.1.0"
edition = "2021"

[lib]
name = "ktile_hotkey"
crate-type = ["cdylib"]

[dependencies]
kbd-global = { version = "0.2.0", features = ["grab"] }
libc = "0.2"

[profile.release]
opt-level = 3
lto = true
